Alembic migrations for HA Finance Intelligence.

Create a new autogenerated migration after changing models:

    cd backend
    alembic revision --autogenerate -m "describe change"

Apply migrations:

    alembic upgrade head
